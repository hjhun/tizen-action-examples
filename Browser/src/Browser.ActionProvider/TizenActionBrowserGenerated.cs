
/*
 * Generated by tidlc 2.10.2.
 */

using System;
using System.Collections.Generic;
using System.Collections.Concurrent;
using System.Threading;
using System.Reflection;
using Tizen.Applications;
using Tizen.Applications.RPCPort;

namespace RPCPort
{
    namespace TizenActionBrowser
    {
        internal class LocalExecution
        {
            object _instance = null;
            MethodInfo _send_result = null;
            MethodInfo _send_request = null;
            MethodInfo _disconnect = null;

            internal bool _online = false;

            internal LocalExecution (object instance)
            {
                Type type = instance.GetType();

                if (type == null)
                    return;

                _send_result = type.GetMethod("OnLemResultEvent", BindingFlags.NonPublic | BindingFlags.Instance);
                if (_send_result == null) { return; }

                _send_request = type.GetMethod("OnLemReceivedEvent", BindingFlags.NonPublic | BindingFlags.Instance);

                _disconnect = type.GetMethod("OnLemDisconnectedEvent", BindingFlags.NonPublic | BindingFlags.Instance);
                if (_disconnect == null) { return; }

                _instance = instance;
                _online = true;

            }

            internal void SendResult(Parcel result)
            {
                _send_result.Invoke(_instance, new object[] {result});
            }

            internal void SendRequest(Parcel result)
            {
                if (_send_request != null)
                    _send_request.Invoke(_instance, new object[] { result });
            }

            internal void Disconnect()
            {
                if (_online)
                _disconnect.Invoke(_instance, null);
                _online = false;
            }
        }

        
        public class RemoteException : Exception
        {
            public RemoteException()
            {
            }

            public RemoteException(string message)
            {
                this.Message = message;
            }

            public RemoteException(string message, int cause)
            {
                this.Message = message;
                this.Cause = cause;
            }

            public new string Message { get; set; }
            public int Cause { get; set; }
        }


        
        public class TizenEntity
        {
            


            public string Id;
            public string Extra;

            public TizenEntity()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntity\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserMedia: TizenEntity
        {
            


            public string Title;
            public int Duration;
            public int Position;
            public bool Playing;

            public TizenEntityBrowserMedia()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserMedia\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Title\":";
                AppendToJson(ref json_str, Title);
        json_str += ",";        json_str += "\"Duration\":";
                AppendToJson(ref json_str, Duration);
        json_str += ",";        json_str += "\"Position\":";
                AppendToJson(ref json_str, Position);
        json_str += ",";        json_str += "\"Playing\":";
                AppendToJson(ref json_str, Playing);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserMediaCommand: TizenEntity
        {
            


            public string Operation;
            public int Position;
            public int Offset;
            public int Ordinal;

            public TizenEntityBrowserMediaCommand()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserMediaCommand\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Operation\":";
                AppendToJson(ref json_str, Operation);
        json_str += ",";        json_str += "\"Position\":";
                AppendToJson(ref json_str, Position);
        json_str += ",";        json_str += "\"Offset\":";
                AppendToJson(ref json_str, Offset);
        json_str += ",";        json_str += "\"Ordinal\":";
                AppendToJson(ref json_str, Ordinal);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserMediaOption: TizenEntity
        {
            


            public string Name;
            public bool Enabled;
            public string RepeatMode;
            public string Speed;
            public string Quality;

            public TizenEntityBrowserMediaOption()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserMediaOption\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
        json_str += ",";        json_str += "\"Enabled\":";
                AppendToJson(ref json_str, Enabled);
        json_str += ",";        json_str += "\"RepeatMode\":";
                AppendToJson(ref json_str, RepeatMode);
        json_str += ",";        json_str += "\"Speed\":";
                AppendToJson(ref json_str, Speed);
        json_str += ",";        json_str += "\"Quality\":";
                AppendToJson(ref json_str, Quality);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityQuery: TizenEntity
        {
            


            public string Keyword;
            public string Category;
            public int Limit;

            public TizenEntityQuery()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityQuery\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Keyword\":";
                AppendToJson(ref json_str, Keyword);
        json_str += ",";        json_str += "\"Category\":";
                AppendToJson(ref json_str, Category);
        json_str += ",";        json_str += "\"Limit\":";
                AppendToJson(ref json_str, Limit);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityWebPageInfo: TizenEntity
        {
            


            public string Url;
            public string Title;
            public string Details;

            public TizenEntityWebPageInfo()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityWebPageInfo\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Url\":";
                AppendToJson(ref json_str, Url);
        json_str += ",";        json_str += "\"Title\":";
                AppendToJson(ref json_str, Title);
        json_str += ",";        json_str += "\"Details\":";
                AppendToJson(ref json_str, Details);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserQuery: TizenEntityQuery
        {
            


            public string Target;
            public List<TizenEntityWebPageInfo> Page;

            public TizenEntityBrowserQuery()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserQuery\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Keyword\":";
                AppendToJson(ref json_str, Keyword);
        json_str += ",";        json_str += "\"Category\":";
                AppendToJson(ref json_str, Category);
        json_str += ",";        json_str += "\"Limit\":";
                AppendToJson(ref json_str, Limit);
        json_str += ",";        json_str += "\"Target\":";
                AppendToJson(ref json_str, Target);
        json_str += ",";        json_str += "\"Page\":";
                AppendToJson(ref json_str, Page);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserScreen: TizenEntity
        {
            


            public string Name;

            public TizenEntityBrowserScreen()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserScreen\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserScreenOption: TizenEntity
        {
            


            public string Name;
            public bool Enabled;
            public string ZoomMode;
            public int ZoomAmount;
            public string Layout;

            public TizenEntityBrowserScreenOption()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserScreenOption\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
        json_str += ",";        json_str += "\"Enabled\":";
                AppendToJson(ref json_str, Enabled);
        json_str += ",";        json_str += "\"ZoomMode\":";
                AppendToJson(ref json_str, ZoomMode);
        json_str += ",";        json_str += "\"ZoomAmount\":";
                AppendToJson(ref json_str, ZoomAmount);
        json_str += ",";        json_str += "\"Layout\":";
                AppendToJson(ref json_str, Layout);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserAnswer: TizenEntity
        {
            


            public string Text;
            public string SourceUrl;

            public TizenEntityBrowserAnswer()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserAnswer\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Text\":";
                AppendToJson(ref json_str, Text);
        json_str += ",";        json_str += "\"SourceUrl\":";
                AppendToJson(ref json_str, SourceUrl);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserPerson: TizenEntity
        {
            


            public string Name;
            public string Role;
            public string Description;

            public TizenEntityBrowserPerson()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserPerson\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
        json_str += ",";        json_str += "\"Role\":";
                AppendToJson(ref json_str, Role);
        json_str += ",";        json_str += "\"Description\":";
                AppendToJson(ref json_str, Description);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserPlace: TizenEntity
        {
            


            public string Name;
            public string Address;
            public string Latitude;
            public string Longitude;

            public TizenEntityBrowserPlace()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserPlace\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
        json_str += ",";        json_str += "\"Address\":";
                AppendToJson(ref json_str, Address);
        json_str += ",";        json_str += "\"Latitude\":";
                AppendToJson(ref json_str, Latitude);
        json_str += ",";        json_str += "\"Longitude\":";
                AppendToJson(ref json_str, Longitude);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserProduct: TizenEntity
        {
            


            public string Name;
            public string Price;
            public string Currency;
            public string Seller;

            public TizenEntityBrowserProduct()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserProduct\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Name\":";
                AppendToJson(ref json_str, Name);
        json_str += ",";        json_str += "\"Price\":";
                AppendToJson(ref json_str, Price);
        json_str += ",";        json_str += "\"Currency\":";
                AppendToJson(ref json_str, Currency);
        json_str += ",";        json_str += "\"Seller\":";
                AppendToJson(ref json_str, Seller);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserSearchResult: TizenEntity
        {
            


            public List<TizenEntityBrowserAnswer> Answer;
            public List<TizenEntityBrowserPlace> Place;
            public List<TizenEntityBrowserProduct> Product;
            public List<TizenEntityBrowserPerson> Person;

            public TizenEntityBrowserSearchResult()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserSearchResult\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Answer\":";
                AppendToJson(ref json_str, Answer);
        json_str += ",";        json_str += "\"Place\":";
                AppendToJson(ref json_str, Place);
        json_str += ",";        json_str += "\"Product\":";
                AppendToJson(ref json_str, Product);
        json_str += ",";        json_str += "\"Person\":";
                AppendToJson(ref json_str, Person);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityBrowserSelection: TizenEntity
        {
            


            public int Ordinal;
            public string Title;
            public string Deictic;

            public TizenEntityBrowserSelection()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityBrowserSelection\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Ordinal\":";
                AppendToJson(ref json_str, Ordinal);
        json_str += ",";        json_str += "\"Title\":";
                AppendToJson(ref json_str, Title);
        json_str += ",";        json_str += "\"Deictic\":";
                AppendToJson(ref json_str, Deictic);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityCalendarEvent: TizenEntity
        {
            


            public string Title;
            public string StartDate;
            public string EndDate;
            public string Note;
            public string Location;

            public TizenEntityCalendarEvent()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityCalendarEvent\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Title\":";
                AppendToJson(ref json_str, Title);
        json_str += ",";        json_str += "\"StartDate\":";
                AppendToJson(ref json_str, StartDate);
        json_str += ",";        json_str += "\"EndDate\":";
                AppendToJson(ref json_str, EndDate);
        json_str += ",";        json_str += "\"Note\":";
                AppendToJson(ref json_str, Note);
        json_str += ",";        json_str += "\"Location\":";
                AppendToJson(ref json_str, Location);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityEngageCommand: TizenEntity
        {
            


            public string Operation;

            public TizenEntityEngageCommand()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityEngageCommand\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Operation\":";
                AppendToJson(ref json_str, Operation);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityNavigationCommand: TizenEntity
        {
            


            public string Operation;
            public string Direction;
            public int Amount;

            public TizenEntityNavigationCommand()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityNavigationCommand\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Operation\":";
                AppendToJson(ref json_str, Operation);
        json_str += ",";        json_str += "\"Direction\":";
                AppendToJson(ref json_str, Direction);
        json_str += ",";        json_str += "\"Amount\":";
                AppendToJson(ref json_str, Amount);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityPageListItem: TizenEntity
        {
            


            public string List;
            public TizenEntityWebPageInfo Page;
            public string Date;
            public int VisitCount;

            public TizenEntityPageListItem()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityPageListItem\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"List\":";
                AppendToJson(ref json_str, List);
        json_str += ",";        json_str += "\"Page\":";
                AppendToJson(ref json_str, Page);
        json_str += ",";        json_str += "\"Date\":";
                AppendToJson(ref json_str, Date);
        json_str += ",";        json_str += "\"VisitCount\":";
                AppendToJson(ref json_str, VisitCount);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityPageListCommand: TizenEntity
        {
            


            public string Operation;
            public TizenEntityPageListItem Item;

            public TizenEntityPageListCommand()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityPageListCommand\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Operation\":";
                AppendToJson(ref json_str, Operation);
        json_str += ",";        json_str += "\"Item\":";
                AppendToJson(ref json_str, Item);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityPresentation
        {
            


            public string Document;
            public string Template;

            public TizenEntityPresentation()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityPresentation\":{";
                json_str += "\"Document\":";
                AppendToJson(ref json_str, Document);
        json_str += ",";        json_str += "\"Template\":";
                AppendToJson(ref json_str, Template);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityStatus
        {
            


            public bool Success;
            public string Reason;

            public TizenEntityStatus()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityStatus\":{";
                json_str += "\"Success\":";
                AppendToJson(ref json_str, Success);
        json_str += ",";        json_str += "\"Reason\":";
                AppendToJson(ref json_str, Reason);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityTab: TizenEntity
        {
            


            public TizenEntityWebPageInfo Page;
            public int Ordinal;
            public bool Focused;
            public bool Secret;

            public TizenEntityTab()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityTab\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Page\":";
                AppendToJson(ref json_str, Page);
        json_str += ",";        json_str += "\"Ordinal\":";
                AppendToJson(ref json_str, Ordinal);
        json_str += ",";        json_str += "\"Focused\":";
                AppendToJson(ref json_str, Focused);
        json_str += ",";        json_str += "\"Secret\":";
                AppendToJson(ref json_str, Secret);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityTabCommand: TizenEntity
        {
            


            public string Operation;
            public List<TizenEntityWebPageInfo> Page;
            public List<TizenEntityTab> Tab;
            public bool Secret;

            public TizenEntityTabCommand()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityTabCommand\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Operation\":";
                AppendToJson(ref json_str, Operation);
        json_str += ",";        json_str += "\"Page\":";
                AppendToJson(ref json_str, Page);
        json_str += ",";        json_str += "\"Tab\":";
                AppendToJson(ref json_str, Tab);
        json_str += ",";        json_str += "\"Secret\":";
                AppendToJson(ref json_str, Secret);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }



        namespace Stub
        {
            public class TizenActionBrowser : StubBase
            {
                

                private List<ServiceBase> _services = new List<ServiceBase>();
                private Type _serviceType;
                private static readonly string _tidlVersion = "2.10.2";
                private Dictionary<MethodId, List<string>> _privilege_map = new Dictionary<MethodId, List<string>>();
                private static TizenActionBrowser _instance;
                private bool _isListen = false;
                private SynchronizationContext _async_context = TizenSynchronizationContext.Current;

                public abstract class ServiceBase
                {
                    internal bool _is_app = true;
                    internal LocalExecution _lem;

                    /// <summary>
                    /// The client app ID
                    /// </summary>
                    public string Sender
                    {
                        get; internal set;
                    }

                    /// <summary>
                    /// The client instance ID
                    /// </summary>
                    public string Instance
                    {
                        get; internal set;
                    }

                    public Port Port
                    {
                        get;
                        internal set;
                    }

                    protected ServiceBase()
                    {

                    }

                    /// <summary>
                    /// Disconnects from the proxy app
                    /// </summary>
                    /// <exception cref="System.InvalidOperationException">
                    /// Thrown when an internal IO error occurrs.
                    /// </exception>
                    public void Disconnect()
                    {
                        if(_lem != null)
                        {
                            _lem.Disconnect();
                        }
                        else
                        {
                            Port?.Disconnect();
                            Port = null;
                        }
                    }

                    /// <summary>
                    /// This method will be called when the client is connected
                    /// </summary>
                    public abstract void OnCreate();

                    /// <summary>
                    /// This method will be called when the client is disconnected
                    /// </summary>
                    public abstract void OnTerminate();

                    public abstract TizenEntityStatus ControlMedia(TizenEntityBrowserMediaCommand browserMediaCommand);

                    public abstract TizenEntityStatus ControlTab(TizenEntityTabCommand tabCommand, out TizenEntityTab result);

                    public abstract TizenEntityStatus Engage(TizenEntityEngageCommand engageCommand);

                    public abstract TizenEntityStatus Exit();

                    public abstract TizenEntityStatus GetCurrentPage(out TizenEntityWebPageInfo result);

                    public abstract TizenEntityStatus GetMedia(out TizenEntityBrowserMedia result);

                    public abstract TizenEntityStatus GetTabs(out List<TizenEntityTab> result);

                    public abstract TizenEntityStatus GoToScreen(TizenEntityBrowserScreen browserScreen);

                    public abstract TizenEntityStatus Navigate(TizenEntityNavigationCommand navigationCommand);

                    public abstract TizenEntityStatus OpenPage(TizenEntityWebPageInfo webPageInfo);

                    public abstract TizenEntityStatus Search(TizenEntityBrowserQuery browserQuery, out TizenEntityBrowserSearchResult result);

                    public abstract TizenEntityStatus SearchPageList(TizenEntityQuery query, out List<TizenEntityPageListItem> result);

                    public abstract TizenEntityStatus SelectItem(TizenEntityBrowserSelection browserSelection);

                    public abstract TizenEntityStatus SetMediaOption(TizenEntityBrowserMediaOption browserMediaOption);

                    public abstract TizenEntityStatus SetScreenOption(TizenEntityBrowserScreenOption browserScreenOption);

                    public abstract TizenEntityStatus ToCalendar(TizenEntityWebPageInfo webPageInfo, out TizenEntityCalendarEvent result);

                    public abstract TizenEntityStatus ToPresentation(TizenEntityWebPageInfo webPageInfo, out TizenEntityPresentation result);

                    public abstract TizenEntityStatus UpdatePageList(TizenEntityPageListCommand pageListCommand, out TizenEntityPageListItem result);

                }

                
                internal class Unit
                {
                    internal int name;
                    internal int type;
                    internal Parcel parcel;
                    internal List<Action<Parcel>> cmds = new List<Action<Parcel>>();
                    internal Unit()
                    {
                    }

                    internal void Serialize(Parcel h)
                    {
                        h.WriteInt(name);
                        h.WriteInt(type);

                        var sizePos = h.GetDataSize();
                        h.Reserve(4);

                        foreach(var cb in cmds)
                        {
                            cb(h);
                        }

                        var curPos = h.GetDataSize();
                        var size = curPos - sizePos - 4;
                        h.SetDataSize(sizePos);
                        h.WriteInt((int)size);
                        h.SetDataSize(curPos);
                    }

                    internal void Deserialize(Parcel h)
                    {
                        name = h.ReadInt();
                        type = h.ReadInt();
                        var size = h.ReadArrayCount();
                        if (size == 0)
                            return;

                        var startPos = h.GetReader();
                        parcel = new Parcel(h, startPos, (uint)size);
                        parcel.Reserve((uint)size);
                        h.SetReader(startPos + (uint)size);
                    }
                }

                internal class UnitMap
                {
                    internal Dictionary<int, Unit> map;
                    internal UnitMap()
                    {
                        map = new Dictionary<int, Unit>();
                    }

                    internal void Serialize(Parcel parcel)
                    {
                        parcel.WriteArrayCount(map.Count & int.MaxValue);
                        foreach (Unit unit in map.Values)
                        {
                            unit.Serialize(parcel);
                        }
                    }

                    internal void Deserialize(Parcel parcel)
                    {
                        int size = parcel.ReadArrayCount();
                        parcel.Pin();

                        for (int i = 0; i < size; i++)
                        {
                            Unit unit = new Unit();
                            unit.Deserialize(parcel);

                            map[unit.name] = unit;
                        }
                    }

                    internal bool ContainsName(int name)
                    {
                        return map.ContainsKey(name);
                    }

                    
                    internal void Read(int name, out TizenEntityBrowserScreenOption value)
                    {
                        value = new TizenEntityBrowserScreenOption();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1076712137 /*TizenEntity::TizenEntityBrowserScreenOption*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserScreenOption : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserScreenOption value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1076712137; /*TizenEntity::TizenEntityBrowserScreenOption*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserMediaOption value)
                    {
                        value = new TizenEntityBrowserMediaOption();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1471000143 /*TizenEntity::TizenEntityBrowserMediaOption*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserMediaOption : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserMediaOption value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1471000143; /*TizenEntity::TizenEntityBrowserMediaOption*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityQuery value)
                    {
                        value = new TizenEntityQuery();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1389800376 /*TizenEntity::TizenEntityQuery*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityQuery : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityQuery value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1389800376; /*TizenEntity::TizenEntityQuery*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserSelection value)
                    {
                        value = new TizenEntityBrowserSelection();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1015347260 /*TizenEntity::TizenEntityBrowserSelection*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserSelection : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserSelection value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1015347260; /*TizenEntity::TizenEntityBrowserSelection*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserSearchResult value)
                    {
                        value = new TizenEntityBrowserSearchResult();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1343530323 /*TizenEntity::TizenEntityBrowserSearchResult*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserSearchResult : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserSearchResult value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1343530323; /*TizenEntity::TizenEntityBrowserSearchResult*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserQuery value)
                    {
                        value = new TizenEntityBrowserQuery();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1509051383 /*TizenEntity::TizenEntityQuery::TizenEntityBrowserQuery*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityQuery::TizenEntityBrowserQuery : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserQuery value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1509051383; /*TizenEntity::TizenEntityQuery::TizenEntityBrowserQuery*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityNavigationCommand value)
                    {
                        value = new TizenEntityNavigationCommand();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 388868631 /*TizenEntity::TizenEntityNavigationCommand*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityNavigationCommand : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityNavigationCommand value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 388868631; /*TizenEntity::TizenEntityNavigationCommand*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserScreen value)
                    {
                        value = new TizenEntityBrowserScreen();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -981625548 /*TizenEntity::TizenEntityBrowserScreen*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserScreen : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserScreen value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -981625548; /*TizenEntity::TizenEntityBrowserScreen*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out List<TizenEntityTab> value)
                    {
                        value = new List<TizenEntityTab>();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 103249674 /*array_TizenEntityTab*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not array_TizenEntityTab : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, List<TizenEntityTab> value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 103249674; /*array_TizenEntityTab*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out CallbackBase value)
                    {
                        value = new CallbackBase(1, false);
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 819322245 /*delegate*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not delegate : " + unit.type);
                            return;
                        }
                        CallbackBase.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, CallbackBase value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 819322245; /*delegate*/
                        unit.cmds.Add((parcel) =>
                        {
                            CallbackBase.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out int value)
                    {
                        value = new int();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 104431 /*int*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not int : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadInt();
                    }

                    internal void Write(int name, int value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 104431; /*int*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteInt(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out bool value)
                    {
                        value = new bool();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 3029738 /*bool*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not bool : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadBool();
                    }

                    internal void Write(int name, bool value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 3029738; /*bool*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteBool(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out string value)
                    {
                        value = new string("");
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -891985903 /*string*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not string : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadString();
                    }

                    internal void Write(int name, string value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -891985903; /*string*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteString(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserMediaCommand value)
                    {
                        value = new TizenEntityBrowserMediaCommand();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -451461745 /*TizenEntity::TizenEntityBrowserMediaCommand*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserMediaCommand : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserMediaCommand value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -451461745; /*TizenEntity::TizenEntityBrowserMediaCommand*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityPageListCommand value)
                    {
                        value = new TizenEntityPageListCommand();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1052460482 /*TizenEntity::TizenEntityPageListCommand*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityPageListCommand : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityPageListCommand value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1052460482; /*TizenEntity::TizenEntityPageListCommand*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityPresentation value)
                    {
                        value = new TizenEntityPresentation();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1161731883 /*TizenEntityPresentation*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntityPresentation : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityPresentation value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1161731883; /*TizenEntityPresentation*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityTabCommand value)
                    {
                        value = new TizenEntityTabCommand();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1160112758 /*TizenEntity::TizenEntityTabCommand*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityTabCommand : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityTabCommand value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1160112758; /*TizenEntity::TizenEntityTabCommand*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityPageListItem value)
                    {
                        value = new TizenEntityPageListItem();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -286606080 /*TizenEntity::TizenEntityPageListItem*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityPageListItem : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityPageListItem value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -286606080; /*TizenEntity::TizenEntityPageListItem*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out List<TizenEntityPageListItem> value)
                    {
                        value = new List<TizenEntityPageListItem>();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 501204139 /*array_TizenEntityPageListItem*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not array_TizenEntityPageListItem : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, List<TizenEntityPageListItem> value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 501204139; /*array_TizenEntityPageListItem*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityBrowserMedia value)
                    {
                        value = new TizenEntityBrowserMedia();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1902502428 /*TizenEntity::TizenEntityBrowserMedia*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityBrowserMedia : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityBrowserMedia value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1902502428; /*TizenEntity::TizenEntityBrowserMedia*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out RemoteException value)
                    {
                        value = new RemoteException();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1223777386 /*remote_exception*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not remote_exception : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, RemoteException value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1223777386; /*remote_exception*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityStatus value)
                    {
                        value = new TizenEntityStatus();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1652879165 /*TizenEntityStatus*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntityStatus : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityStatus value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1652879165; /*TizenEntityStatus*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityTab value)
                    {
                        value = new TizenEntityTab();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 521460501 /*TizenEntity::TizenEntityTab*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityTab : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityTab value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 521460501; /*TizenEntity::TizenEntityTab*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityCalendarEvent value)
                    {
                        value = new TizenEntityCalendarEvent();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1474520708 /*TizenEntity::TizenEntityCalendarEvent*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityCalendarEvent : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityCalendarEvent value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1474520708; /*TizenEntity::TizenEntityCalendarEvent*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityEngageCommand value)
                    {
                        value = new TizenEntityEngageCommand();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1403189290 /*TizenEntity::TizenEntityEngageCommand*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityEngageCommand : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityEngageCommand value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1403189290; /*TizenEntity::TizenEntityEngageCommand*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityWebPageInfo value)
                    {
                        value = new TizenEntityWebPageInfo();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1183486673 /*TizenEntity::TizenEntityWebPageInfo*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityWebPageInfo : " + unit.type);
                            return;
                        }
                        TizenActionBrowser.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityWebPageInfo value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1183486673; /*TizenEntity::TizenEntityWebPageInfo*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionBrowser.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                }

                
                public class CallbackBase
                {
                    internal int Id;
                    internal int SeqId;
                    internal bool Once;
                    private static volatile int _seqNum = 0;

                    public string Tag
                    {
                        get
                        {
                            return Id.ToString() + "::" + SeqId.ToString();
                        }
                    }

                    public CallbackBase(int delegateId, bool once)
                    {
                        Id = delegateId;
                        SeqId = _seqNum++;
                        Once = once;
                    }

                    internal virtual void OnReceivedEvent(UnitMap map) {}

                    internal static void Serialize(Parcel h, CallbackBase param)
                    {
                        h.WriteInt(param.Id);
                        h.WriteInt(param.SeqId);
                        h.WriteBool(param.Once);
                    }

                    internal static void Deserialize(Parcel h, CallbackBase param)
                    {
                        param.Id = h.ReadInt();
                        param.SeqId = h.ReadInt();
                        param.Once = h.ReadBool();
                    }
                }



                
                
                private enum MethodId : int
                {
                    __Result = 0,
                    __Callback = 1,
                    ControlMedia = 2,
                    ControlTab = 3,
                    Engage = 4,
                    Exit = 5,
                    GetCurrentPage = 6,
                    GetMedia = 7,
                    GetTabs = 8,
                    GoToScreen = 9,
                    Navigate = 10,
                    OpenPage = 11,
                    Search = 12,
                    SearchPageList = 13,
                    SelectItem = 14,
                    SetMediaOption = 15,
                    SetScreenOption = 16,
                    ToCalendar = 17,
                    ToPresentation = 18,
                    UpdatePageList = 19,
                }

                
                private static void Serialize(Parcel h, TizenEntity param)
                {
                    h.WriteString(param.Id);
                    h.WriteString(param.Extra);
                }

                private static void Deserialize(Parcel h, TizenEntity param)
                {
                    var Id = h.ReadString();
                    param.Id = Id;
                    var Extra = h.ReadString();
                    param.Extra = Extra;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserMedia param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Title);
                    h.WriteInt(param.Duration);
                    h.WriteInt(param.Position);
                    h.WriteBool(param.Playing);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserMedia param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Title = h.ReadString();
                    param.Title = Title;
                    var Duration = h.ReadInt();
                    param.Duration = Duration;
                    var Position = h.ReadInt();
                    param.Position = Position;
                    var Playing = h.ReadBool();
                    param.Playing = Playing;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserMediaCommand param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Operation);
                    h.WriteInt(param.Position);
                    h.WriteInt(param.Offset);
                    h.WriteInt(param.Ordinal);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserMediaCommand param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Operation = h.ReadString();
                    param.Operation = Operation;
                    var Position = h.ReadInt();
                    param.Position = Position;
                    var Offset = h.ReadInt();
                    param.Offset = Offset;
                    var Ordinal = h.ReadInt();
                    param.Ordinal = Ordinal;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserMediaOption param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                    h.WriteBool(param.Enabled);
                    h.WriteString(param.RepeatMode);
                    h.WriteString(param.Speed);
                    h.WriteString(param.Quality);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserMediaOption param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                    var Enabled = h.ReadBool();
                    param.Enabled = Enabled;
                    var RepeatMode = h.ReadString();
                    param.RepeatMode = RepeatMode;
                    var Speed = h.ReadString();
                    param.Speed = Speed;
                    var Quality = h.ReadString();
                    param.Quality = Quality;
                }

                private static void Serialize(Parcel h, TizenEntityQuery param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Keyword);
                    h.WriteString(param.Category);
                    h.WriteInt(param.Limit);
                }

                private static void Deserialize(Parcel h, TizenEntityQuery param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Keyword = h.ReadString();
                    param.Keyword = Keyword;
                    var Category = h.ReadString();
                    param.Category = Category;
                    var Limit = h.ReadInt();
                    param.Limit = Limit;
                }

                private static void Serialize(Parcel h, TizenEntityWebPageInfo param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Url);
                    h.WriteString(param.Title);
                    h.WriteString(param.Details);
                }

                private static void Deserialize(Parcel h, TizenEntityWebPageInfo param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Url = h.ReadString();
                    param.Url = Url;
                    var Title = h.ReadString();
                    param.Title = Title;
                    var Details = h.ReadString();
                    param.Details = Details;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserQuery param)
                {
                    Serialize(h, (TizenEntityQuery)param);
                    h.WriteString(param.Target);
                    Serialize(h, param.Page);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserQuery param)
                {
                    Deserialize(h, (TizenEntityQuery)param);
                    var Target = h.ReadString();
                    param.Target = Target;
                    param.Page = new List<TizenEntityWebPageInfo>();
                    Deserialize(h, param.Page);
                }

                private static void Serialize(Parcel h, TizenEntityBrowserScreen param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserScreen param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserScreenOption param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                    h.WriteBool(param.Enabled);
                    h.WriteString(param.ZoomMode);
                    h.WriteInt(param.ZoomAmount);
                    h.WriteString(param.Layout);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserScreenOption param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                    var Enabled = h.ReadBool();
                    param.Enabled = Enabled;
                    var ZoomMode = h.ReadString();
                    param.ZoomMode = ZoomMode;
                    var ZoomAmount = h.ReadInt();
                    param.ZoomAmount = ZoomAmount;
                    var Layout = h.ReadString();
                    param.Layout = Layout;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserAnswer param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Text);
                    h.WriteString(param.SourceUrl);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserAnswer param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Text = h.ReadString();
                    param.Text = Text;
                    var SourceUrl = h.ReadString();
                    param.SourceUrl = SourceUrl;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserPerson param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                    h.WriteString(param.Role);
                    h.WriteString(param.Description);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserPerson param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                    var Role = h.ReadString();
                    param.Role = Role;
                    var Description = h.ReadString();
                    param.Description = Description;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserPlace param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                    h.WriteString(param.Address);
                    h.WriteString(param.Latitude);
                    h.WriteString(param.Longitude);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserPlace param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                    var Address = h.ReadString();
                    param.Address = Address;
                    var Latitude = h.ReadString();
                    param.Latitude = Latitude;
                    var Longitude = h.ReadString();
                    param.Longitude = Longitude;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserProduct param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Name);
                    h.WriteString(param.Price);
                    h.WriteString(param.Currency);
                    h.WriteString(param.Seller);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserProduct param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Name = h.ReadString();
                    param.Name = Name;
                    var Price = h.ReadString();
                    param.Price = Price;
                    var Currency = h.ReadString();
                    param.Currency = Currency;
                    var Seller = h.ReadString();
                    param.Seller = Seller;
                }

                private static void Serialize(Parcel h, TizenEntityBrowserSearchResult param)
                {
                    Serialize(h, (TizenEntity)param);
                    Serialize(h, param.Answer);
                    Serialize(h, param.Place);
                    Serialize(h, param.Product);
                    Serialize(h, param.Person);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserSearchResult param)
                {
                    Deserialize(h, (TizenEntity)param);
                    param.Answer = new List<TizenEntityBrowserAnswer>();
                    Deserialize(h, param.Answer);
                    param.Place = new List<TizenEntityBrowserPlace>();
                    Deserialize(h, param.Place);
                    param.Product = new List<TizenEntityBrowserProduct>();
                    Deserialize(h, param.Product);
                    param.Person = new List<TizenEntityBrowserPerson>();
                    Deserialize(h, param.Person);
                }

                private static void Serialize(Parcel h, TizenEntityBrowserSelection param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteInt(param.Ordinal);
                    h.WriteString(param.Title);
                    h.WriteString(param.Deictic);
                }

                private static void Deserialize(Parcel h, TizenEntityBrowserSelection param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Ordinal = h.ReadInt();
                    param.Ordinal = Ordinal;
                    var Title = h.ReadString();
                    param.Title = Title;
                    var Deictic = h.ReadString();
                    param.Deictic = Deictic;
                }

                private static void Serialize(Parcel h, TizenEntityCalendarEvent param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Title);
                    h.WriteString(param.StartDate);
                    h.WriteString(param.EndDate);
                    h.WriteString(param.Note);
                    h.WriteString(param.Location);
                }

                private static void Deserialize(Parcel h, TizenEntityCalendarEvent param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Title = h.ReadString();
                    param.Title = Title;
                    var StartDate = h.ReadString();
                    param.StartDate = StartDate;
                    var EndDate = h.ReadString();
                    param.EndDate = EndDate;
                    var Note = h.ReadString();
                    param.Note = Note;
                    var Location = h.ReadString();
                    param.Location = Location;
                }

                private static void Serialize(Parcel h, TizenEntityEngageCommand param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Operation);
                }

                private static void Deserialize(Parcel h, TizenEntityEngageCommand param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Operation = h.ReadString();
                    param.Operation = Operation;
                }

                private static void Serialize(Parcel h, TizenEntityNavigationCommand param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Operation);
                    h.WriteString(param.Direction);
                    h.WriteInt(param.Amount);
                }

                private static void Deserialize(Parcel h, TizenEntityNavigationCommand param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Operation = h.ReadString();
                    param.Operation = Operation;
                    var Direction = h.ReadString();
                    param.Direction = Direction;
                    var Amount = h.ReadInt();
                    param.Amount = Amount;
                }

                private static void Serialize(Parcel h, TizenEntityPageListItem param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.List);
                    Serialize(h, param.Page);
                    h.WriteString(param.Date);
                    h.WriteInt(param.VisitCount);
                }

                private static void Deserialize(Parcel h, TizenEntityPageListItem param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var List = h.ReadString();
                    param.List = List;
                    param.Page = new TizenEntityWebPageInfo();
                    Deserialize(h, param.Page);
                    var Date = h.ReadString();
                    param.Date = Date;
                    var VisitCount = h.ReadInt();
                    param.VisitCount = VisitCount;
                }

                private static void Serialize(Parcel h, TizenEntityPageListCommand param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Operation);
                    Serialize(h, param.Item);
                }

                private static void Deserialize(Parcel h, TizenEntityPageListCommand param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Operation = h.ReadString();
                    param.Operation = Operation;
                    param.Item = new TizenEntityPageListItem();
                    Deserialize(h, param.Item);
                }

                private static void Serialize(Parcel h, TizenEntityPresentation param)
                {
                    h.WriteString(param.Document);
                    h.WriteString(param.Template);
                }

                private static void Deserialize(Parcel h, TizenEntityPresentation param)
                {
                    var Document = h.ReadString();
                    param.Document = Document;
                    var Template = h.ReadString();
                    param.Template = Template;
                }

                private static void Serialize(Parcel h, TizenEntityStatus param)
                {
                    h.WriteBool(param.Success);
                    h.WriteString(param.Reason);
                }

                private static void Deserialize(Parcel h, TizenEntityStatus param)
                {
                    var Success = h.ReadBool();
                    param.Success = Success;
                    var Reason = h.ReadString();
                    param.Reason = Reason;
                }

                private static void Serialize(Parcel h, TizenEntityTab param)
                {
                    Serialize(h, (TizenEntity)param);
                    Serialize(h, param.Page);
                    h.WriteInt(param.Ordinal);
                    h.WriteBool(param.Focused);
                    h.WriteBool(param.Secret);
                }

                private static void Deserialize(Parcel h, TizenEntityTab param)
                {
                    Deserialize(h, (TizenEntity)param);
                    param.Page = new TizenEntityWebPageInfo();
                    Deserialize(h, param.Page);
                    var Ordinal = h.ReadInt();
                    param.Ordinal = Ordinal;
                    var Focused = h.ReadBool();
                    param.Focused = Focused;
                    var Secret = h.ReadBool();
                    param.Secret = Secret;
                }

                private static void Serialize(Parcel h, TizenEntityTabCommand param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Operation);
                    Serialize(h, param.Page);
                    Serialize(h, param.Tab);
                    h.WriteBool(param.Secret);
                }

                private static void Deserialize(Parcel h, TizenEntityTabCommand param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Operation = h.ReadString();
                    param.Operation = Operation;
                    param.Page = new List<TizenEntityWebPageInfo>();
                    Deserialize(h, param.Page);
                    param.Tab = new List<TizenEntityTab>();
                    Deserialize(h, param.Tab);
                    var Secret = h.ReadBool();
                    param.Secret = Secret;
                }

                
                private static void Serialize(Parcel h, List<TizenEntityBrowserAnswer> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityBrowserAnswer> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityBrowserAnswer();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityBrowserPerson> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityBrowserPerson> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityBrowserPerson();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityBrowserPlace> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityBrowserPlace> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityBrowserPlace();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityBrowserProduct> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityBrowserProduct> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityBrowserProduct();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityPageListItem> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityPageListItem> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityPageListItem();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityTab> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityTab> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityTab();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<TizenEntityWebPageInfo> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityWebPageInfo> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityWebPageInfo();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                
                private static void Serialize(Parcel h, RemoteException param)
                {
                    h.WriteInt(param.Cause);
                    h.WriteString(param.Message);
                }

                private static void Deserialize(Parcel h, RemoteException param)
                {
                    param.Cause = h.ReadInt();
                    param.Message = h.ReadString();
                }


                private bool CheckPrivilege(MethodId id, ServiceBase b)
                {
                    if (_privilege_map.ContainsKey(id) == false)
                        return true;

                    var privileges = _privilege_map[id];
                    bool has;
                    foreach (var item in privileges)
                    {
                        has = HasPrivilegeLocal(b.Sender, item);
                        if (has == false) {
                            Tizen.Log.Error("RPC_PORT", "Permission denied. " + b.Sender + " : " + item);
                            return false;
                        }
                    }

                    return true;
                }

                protected override bool OnReceivedEvent(string sender, string instance, Port port)
                {
                    Parcel p;

                    try
                    {
                        p = new Parcel(port);
                    }
                    catch (InvalidIOException)
                    {
                        return false;
                    }

                    return OnReceivedEvent(instance, port, p);
                }

                internal void OnLemReceivedEvent(string instance, Parcel p)
                {
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((TizenActionBrowser)data).OnReceivedEvent(instance, null, p);
                        }, this);
                    }
                    else
                    {
                        OnReceivedEvent(instance, null, p);
                    }
                }

                internal  bool OnReceivedEvent(string instance, Port port, Parcel p)
                {
                    try
                    {
                        ServiceBase b = null;

                        foreach (var i in  _services)
                        {
                            if (i.Instance.Equals(instance))
                            {
                                b = i;
                                break;
                            }
                        }

                        if (b == null)
                        {
                            return false;
                        }

                        Port cb_port;
                        if (b._lem != null)
                            cb_port = null;
                        else
                            cb_port = GetPort(Port.Type.Callback, instance);

                        var result = new Parcel();

                        int cmd;
                        UnitMap map = new UnitMap();
                        map.Deserialize(p);

                        map.Read(1355467489 /*[METHOD]*/, out cmd);

                        switch ((MethodId)cmd)
                        {
                            case MethodId.ControlMedia:
                            {
                                map.Read(212541967 /*browserMediaCommand*/, out TizenEntityBrowserMediaCommand param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.ControlMedia(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.ControlTab:
                            {
                                map.Read(1061615094 /*tabCommand*/, out TizenEntityTabCommand param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.ControlTab(param1, out TizenEntityTab param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.Engage:
                            {
                                map.Read(521944234 /*engageCommand*/, out TizenEntityEngageCommand param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.Engage(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.Exit:
                            {
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.Exit();
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.GetCurrentPage:
                            {
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.GetCurrentPage(out TizenEntityWebPageInfo param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param1);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.GetMedia:
                            {
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.GetMedia(out TizenEntityBrowserMedia param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param1);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.GetTabs:
                            {
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.GetTabs(out List<TizenEntityTab> param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param1);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.GoToScreen:
                            {
                                map.Read(-1862870604 /*browserScreen*/, out TizenEntityBrowserScreen param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.GoToScreen(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.Navigate:
                            {
                                map.Read(1131458199 /*navigationCommand*/, out TizenEntityNavigationCommand param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.Navigate(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.OpenPage:
                            {
                                map.Read(-1869940911 /*webPageInfo*/, out TizenEntityWebPageInfo param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.OpenPage(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.Search:
                            {
                                map.Read(1739699840 /*browserQuery*/, out TizenEntityBrowserQuery param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.Search(param1, out TizenEntityBrowserSearchResult param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.SearchPageList:
                            {
                                map.Read(107944136 /*query*/, out TizenEntityQuery param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.SearchPageList(param1, out List<TizenEntityPageListItem> param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.SelectItem:
                            {
                                map.Read(948269892 /*browserSelection*/, out TizenEntityBrowserSelection param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.SelectItem(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.SetMediaOption:
                            {
                                map.Read(74439985 /*browserMediaOption*/, out TizenEntityBrowserMediaOption param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.SetMediaOption(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.SetScreenOption:
                            {
                                map.Read(1740715849 /*browserScreenOption*/, out TizenEntityBrowserScreenOption param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.SetScreenOption(param1);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.ToCalendar:
                            {
                                map.Read(-1869940911 /*webPageInfo*/, out TizenEntityWebPageInfo param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.ToCalendar(param1, out TizenEntityCalendarEvent param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.ToPresentation:
                            {
                                map.Read(-1869940911 /*webPageInfo*/, out TizenEntityWebPageInfo param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.ToPresentation(param1, out TizenEntityPresentation param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.UpdatePageList:
                            {
                                map.Read(-1820401986 /*pageListCommand*/, out TizenEntityPageListCommand param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.UpdatePageList(param1, out TizenEntityPageListItem param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }

                            default:
                                return false;
                        }

                        return true;
                    }
                    catch (InvalidIOException)
                    {
                        return false;
                    }
                    finally
                    {
                        p.Dispose();
                    }
                }

                protected override void OnConnectedEvent(string sender, string instance)
                {
                    ServiceBase s = Activator.CreateInstance(_serviceType) as ServiceBase;
                    s.Sender = sender;
                    s.Instance = instance;
                    s.Port = GetPort(Port.Type.Callback, instance);
                    _services.Add(s);
                    s.OnCreate();
                }

                internal void OnLemConnectedEvent(object proxy_instance, string sender, string instance)
                {
                    ServiceBase s = Activator.CreateInstance(_serviceType) as ServiceBase;
                    s.Sender = sender;
                    s.Instance = instance;
                    s._lem = new LocalExecution(proxy_instance);
                    _services.Add(s);
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((ServiceBase)data).OnCreate();
                        }, s);
                    }
                    else
                    {
                        s.OnCreate();
                    }
                }

                protected override void OnDisconnectedEvent(string sender, string instance)
                {
                    foreach (var i in _services)
                    {
                        if (i.Instance.Equals(instance))
                        {
                            if (i._lem != null)
                                i._lem._online = false;

                            i.OnTerminate();
                            _services.Remove(i);
                            break;
                        }
                    }
                }

                internal void OnLemDisconnectedEvent(string sender, string instance)
                {
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((TizenActionBrowser)data).OnDisconnectedEvent(sender, instance);
                        }, this);
                    }
                    else
                    {
                        OnDisconnectedEvent(sender, instance);
                    }
                }

                internal static TizenActionBrowser GetInstance()
                {
                    return _instance;
                }

                internal bool GetListenStatus()
                {
                    return _isListen;
                }

                public TizenActionBrowser() : base("TizenActionBrowser")
                {
                    _instance = this;


                }

                /// <summary>
                /// Listens to client apps
                /// </summary>
                /// <param name="serviceType">The type object for making service instances</param>
                /// <exception cref="InvalidIOException">
                /// Thrown when internal I/O error happen.
                /// </exception>
                /// <exception cref="ArgumentException">
                /// Thrown when serviceType is invalid.
                /// </exception>
                public void Listen(Type serviceType)
                {
                    if (!typeof(ServiceBase).IsAssignableFrom(serviceType))
                        throw new ArgumentException("Invalid type");
                    _serviceType = serviceType;
                    Listen();
                    _isListen = true;
                }

                /// <summary>
                /// Gets service objects which are connected
                /// </summary>
                /// <returns>The enumerable service objects which are connected</returns>
                public IEnumerable<ServiceBase> GetServices()
                {
                    return _services;
                }


                protected override void OnTerminatedEvent()
                {
                    foreach (var i in _services)
                    {
                        if (i._lem != null)
                        i._lem._online = false;
                        i.OnTerminate();
                    }
                }

            }
        }
    }
}

