
/*
 * Generated by tidlc 2.10.2.
 */

using System;
using System.Collections.Generic;
using System.Collections.Concurrent;
using System.Threading;
using System.Reflection;
using Tizen.Applications;
using Tizen.Applications.RPCPort;

namespace RPCPort
{
    namespace CalendarCustomActionProvider
    {
        internal class LocalExecution
        {
            object _instance = null;
            MethodInfo _send_result = null;
            MethodInfo _send_request = null;
            MethodInfo _disconnect = null;

            internal bool _online = false;

            internal LocalExecution (object instance)
            {
                Type type = instance.GetType();

                if (type == null)
                    return;

                _send_result = type.GetMethod("OnLemResultEvent", BindingFlags.NonPublic | BindingFlags.Instance);
                if (_send_result == null) { return; }

                _send_request = type.GetMethod("OnLemReceivedEvent", BindingFlags.NonPublic | BindingFlags.Instance);

                _disconnect = type.GetMethod("OnLemDisconnectedEvent", BindingFlags.NonPublic | BindingFlags.Instance);
                if (_disconnect == null) { return; }

                _instance = instance;
                _online = true;

            }

            internal void SendResult(Parcel result)
            {
                _send_result.Invoke(_instance, new object[] {result});
            }

            internal void SendRequest(Parcel result)
            {
                if (_send_request != null)
                    _send_request.Invoke(_instance, new object[] { result });
            }

            internal void Disconnect()
            {
                if (_online)
                _disconnect.Invoke(_instance, null);
                _online = false;
            }
        }

        
        public class RemoteException : Exception
        {
            public RemoteException()
            {
            }

            public RemoteException(string message)
            {
                this.Message = message;
            }

            public RemoteException(string message, int cause)
            {
                this.Message = message;
                this.Cause = cause;
            }

            public new string Message { get; set; }
            public int Cause { get; set; }
        }


        
        public class TizenEntity
        {
            


            public string Id;
            public string Extra;

            public TizenEntity()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntity\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityQuery: TizenEntity
        {
            


            public string Keyword;
            public string Category;
            public int Limit;

            public TizenEntityQuery()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityQuery\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Keyword\":";
                AppendToJson(ref json_str, Keyword);
        json_str += ",";        json_str += "\"Category\":";
                AppendToJson(ref json_str, Category);
        json_str += ",";        json_str += "\"Limit\":";
                AppendToJson(ref json_str, Limit);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityCalendarQuery: TizenEntityQuery
        {
            


            public string StartDate;
            public string EndDate;

            public TizenEntityCalendarQuery()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityCalendarQuery\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Keyword\":";
                AppendToJson(ref json_str, Keyword);
        json_str += ",";        json_str += "\"Category\":";
                AppendToJson(ref json_str, Category);
        json_str += ",";        json_str += "\"Limit\":";
                AppendToJson(ref json_str, Limit);
        json_str += ",";        json_str += "\"StartDate\":";
                AppendToJson(ref json_str, StartDate);
        json_str += ",";        json_str += "\"EndDate\":";
                AppendToJson(ref json_str, EndDate);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class CalendarEntitySearchQuery: TizenEntityCalendarQuery
        {
            


            public bool SearchTitle;
            public bool SearchLocation;
            public bool SearchNote;

            public CalendarEntitySearchQuery()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"CalendarEntitySearchQuery\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Keyword\":";
                AppendToJson(ref json_str, Keyword);
        json_str += ",";        json_str += "\"Category\":";
                AppendToJson(ref json_str, Category);
        json_str += ",";        json_str += "\"Limit\":";
                AppendToJson(ref json_str, Limit);
        json_str += ",";        json_str += "\"StartDate\":";
                AppendToJson(ref json_str, StartDate);
        json_str += ",";        json_str += "\"EndDate\":";
                AppendToJson(ref json_str, EndDate);
        json_str += ",";        json_str += "\"SearchTitle\":";
                AppendToJson(ref json_str, SearchTitle);
        json_str += ",";        json_str += "\"SearchLocation\":";
                AppendToJson(ref json_str, SearchLocation);
        json_str += ",";        json_str += "\"SearchNote\":";
                AppendToJson(ref json_str, SearchNote);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityCalendarEvent: TizenEntity
        {
            


            public string Title;
            public string StartDate;
            public string EndDate;
            public string Note;
            public string Location;

            public TizenEntityCalendarEvent()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityCalendarEvent\":{";
                json_str += "\"Id\":";
                AppendToJson(ref json_str, Id);
        json_str += ",";        json_str += "\"Extra\":";
                AppendToJson(ref json_str, Extra);
        json_str += ",";        json_str += "\"Title\":";
                AppendToJson(ref json_str, Title);
        json_str += ",";        json_str += "\"StartDate\":";
                AppendToJson(ref json_str, StartDate);
        json_str += ",";        json_str += "\"EndDate\":";
                AppendToJson(ref json_str, EndDate);
        json_str += ",";        json_str += "\"Note\":";
                AppendToJson(ref json_str, Note);
        json_str += ",";        json_str += "\"Location\":";
                AppendToJson(ref json_str, Location);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }

        public class TizenEntityStatus
        {
            


            public bool Success;
            public string Reason;

            public TizenEntityStatus()
            {
            }

            public string ToJson()
            {
                string json_str = "{\"TizenEntityStatus\":{";
                json_str += "\"Success\":";
                AppendToJson(ref json_str, Success);
        json_str += ",";        json_str += "\"Reason\":";
                AppendToJson(ref json_str, Reason);
                json_str += "}}";
                return json_str;
            }

            private static string JsonEscape(string str)
            {
                if (str == null) return "";
                var sb = new System.Text.StringBuilder(str.Length);
                foreach (char c in str)
                {
                    switch (c)
                    {
                        case '\\': sb.Append("\\\\"); break;
                        case '"': sb.Append("\\\""); break;
                        case '\b': sb.Append("\\b"); break;
                        case '\f': sb.Append("\\f"); break;
                        case '\n': sb.Append("\\n"); break;
                        case '\r': sb.Append("\\r"); break;
                        case '\t': sb.Append("\\t"); break;
                        default:
                            if (c < 0x20)
                                sb.Append("\\u").Append(((int)c).ToString("x4"));
                            else
                                sb.Append(c);
                            break;
                    }
                }
                return sb.ToString();
            }

            private static void AppendToJson(ref string json_str, object val)
            {
                if (val == null)
                {
                    json_str += "null";
                    return;
                }
                var type = val.GetType();
                if (type.IsEnum)
                {
                    json_str += ((int)val).ToString();
                }
                else if (val is bool b)
                {
                    json_str += b ? "true" : "false";
                }
                else if (val is string s)
                {
                    json_str += "\"" + JsonEscape(s) + "\"";
                }
                else if (val is float f)
                {
                    json_str += f.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is double d)
                {
                    json_str += d.ToString(System.Globalization.CultureInfo.InvariantCulture);
                }
                else if (val is sbyte || val is byte || val is short || val is ushort || val is int || val is uint || val is long || val is ulong)
                {
                    json_str += val.ToString();
                }
                else
                {
                    var method = type.GetMethod("ToJson");
                    if (method != null)
                    {
                        json_str += method.Invoke(val, null);
                    }
                    else if (val is System.Collections.IDictionary dict)
                    {
                        json_str += "{";
                        bool first = true;
                        foreach (System.Collections.DictionaryEntry entry in dict)
                        {
                            if (!first) json_str += ",";
                            string key_str = "";
                            AppendToJson(ref key_str, entry.Key);
                            if (!key_str.StartsWith("\""))
                            {
                                key_str = "\"" + key_str + "\"";
                            }
                            json_str += key_str + ":";
                            AppendToJson(ref json_str, entry.Value);
                            first = false;
                        }
                        json_str += "}";
                    }
                    else if (val is System.Collections.IEnumerable enumerable)
                    {
                        json_str += "[";
                        bool first = true;
                        foreach (var item in enumerable)
                        {
                            if (!first) json_str += ",";
                            AppendToJson(ref json_str, item);
                            first = false;
                        }
                        json_str += "]";
                    }
                    else
                    {
                        json_str += "null";
                    }
                }
            }

        }



        namespace Stub
        {
            public class TizenActionCalendarCustom : StubBase
            {
                

                private List<ServiceBase> _services = new List<ServiceBase>();
                private Type _serviceType;
                private static readonly string _tidlVersion = "2.10.2";
                private Dictionary<MethodId, List<string>> _privilege_map = new Dictionary<MethodId, List<string>>();
                private static TizenActionCalendarCustom _instance;
                private bool _isListen = false;
                private SynchronizationContext _async_context = TizenSynchronizationContext.Current;

                public abstract class ServiceBase
                {
                    internal bool _is_app = true;
                    internal LocalExecution _lem;

                    /// <summary>
                    /// The client app ID
                    /// </summary>
                    public string Sender
                    {
                        get; internal set;
                    }

                    /// <summary>
                    /// The client instance ID
                    /// </summary>
                    public string Instance
                    {
                        get; internal set;
                    }

                    public Port Port
                    {
                        get;
                        internal set;
                    }

                    protected ServiceBase()
                    {

                    }

                    /// <summary>
                    /// Disconnects from the proxy app
                    /// </summary>
                    /// <exception cref="System.InvalidOperationException">
                    /// Thrown when an internal IO error occurrs.
                    /// </exception>
                    public void Disconnect()
                    {
                        if(_lem != null)
                        {
                            _lem.Disconnect();
                        }
                        else
                        {
                            Port?.Disconnect();
                            Port = null;
                        }
                    }

                    /// <summary>
                    /// This method will be called when the client is connected
                    /// </summary>
                    public abstract void OnCreate();

                    /// <summary>
                    /// This method will be called when the client is disconnected
                    /// </summary>
                    public abstract void OnTerminate();

                    public abstract TizenEntityStatus GetEventByIds(List<string> ids, out List<TizenEntityCalendarEvent> result, out List<string> unresolvedIds);

                    public abstract TizenEntityStatus SearchInPeriod(CalendarEntitySearchQuery searchQuery, out List<TizenEntityCalendarEvent> result);

                }

                
                internal class Unit
                {
                    internal int name;
                    internal int type;
                    internal Parcel parcel;
                    internal List<Action<Parcel>> cmds = new List<Action<Parcel>>();
                    internal Unit()
                    {
                    }

                    internal void Serialize(Parcel h)
                    {
                        h.WriteInt(name);
                        h.WriteInt(type);

                        var sizePos = h.GetDataSize();
                        h.Reserve(4);

                        foreach(var cb in cmds)
                        {
                            cb(h);
                        }

                        var curPos = h.GetDataSize();
                        var size = curPos - sizePos - 4;
                        h.SetDataSize(sizePos);
                        h.WriteInt((int)size);
                        h.SetDataSize(curPos);
                    }

                    internal void Deserialize(Parcel h)
                    {
                        name = h.ReadInt();
                        type = h.ReadInt();
                        var size = h.ReadArrayCount();
                        if (size == 0)
                            return;

                        var startPos = h.GetReader();
                        parcel = new Parcel(h, startPos, (uint)size);
                        parcel.Reserve((uint)size);
                        h.SetReader(startPos + (uint)size);
                    }
                }

                internal class UnitMap
                {
                    internal Dictionary<int, Unit> map;
                    internal UnitMap()
                    {
                        map = new Dictionary<int, Unit>();
                    }

                    internal void Serialize(Parcel parcel)
                    {
                        parcel.WriteArrayCount(map.Count & int.MaxValue);
                        foreach (Unit unit in map.Values)
                        {
                            unit.Serialize(parcel);
                        }
                    }

                    internal void Deserialize(Parcel parcel)
                    {
                        int size = parcel.ReadArrayCount();
                        parcel.Pin();

                        for (int i = 0; i < size; i++)
                        {
                            Unit unit = new Unit();
                            unit.Deserialize(parcel);

                            map[unit.name] = unit;
                        }
                    }

                    internal bool ContainsName(int name)
                    {
                        return map.ContainsKey(name);
                    }

                    
                    internal void Read(int name, out List<TizenEntityCalendarEvent> value)
                    {
                        value = new List<TizenEntityCalendarEvent>();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 1472759601 /*array_TizenEntityCalendarEvent*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not array_TizenEntityCalendarEvent : " + unit.type);
                            return;
                        }
                        TizenActionCalendarCustom.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, List<TizenEntityCalendarEvent> value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 1472759601; /*array_TizenEntityCalendarEvent*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionCalendarCustom.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out TizenEntityStatus value)
                    {
                        value = new TizenEntityStatus();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1652879165 /*TizenEntityStatus*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntityStatus : " + unit.type);
                            return;
                        }
                        TizenActionCalendarCustom.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, TizenEntityStatus value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1652879165; /*TizenEntityStatus*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionCalendarCustom.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out string value)
                    {
                        value = new string("");
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -891985903 /*string*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not string : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadString();
                    }

                    internal void Write(int name, string value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -891985903; /*string*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteString(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out List<string> value)
                    {
                        value = new List<string>();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1266858825 /*array_string*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not array_string : " + unit.type);
                            return;
                        }
                        TizenActionCalendarCustom.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, List<string> value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1266858825; /*array_string*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionCalendarCustom.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out int value)
                    {
                        value = new int();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 104431 /*int*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not int : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadInt();
                    }

                    internal void Write(int name, int value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 104431; /*int*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteInt(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out CalendarEntitySearchQuery value)
                    {
                        value = new CalendarEntitySearchQuery();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -566823746 /*TizenEntity::TizenEntityQuery::TizenEntityCalendarQuery::CalendarEntitySearchQuery*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not TizenEntity::TizenEntityQuery::TizenEntityCalendarQuery::CalendarEntitySearchQuery : " + unit.type);
                            return;
                        }
                        TizenActionCalendarCustom.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, CalendarEntitySearchQuery value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -566823746; /*TizenEntity::TizenEntityQuery::TizenEntityCalendarQuery::CalendarEntitySearchQuery*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionCalendarCustom.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out bool value)
                    {
                        value = new bool();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 3029738 /*bool*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not bool : " + unit.type);
                            return;
                        }
                        value = unit.parcel.ReadBool();
                    }

                    internal void Write(int name, bool value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 3029738; /*bool*/
                        unit.cmds.Add((parcel) =>
                        {
                            parcel.WriteBool(value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out CallbackBase value)
                    {
                        value = new CallbackBase(1, false);
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != 819322245 /*delegate*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not delegate : " + unit.type);
                            return;
                        }
                        CallbackBase.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, CallbackBase value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = 819322245; /*delegate*/
                        unit.cmds.Add((parcel) =>
                        {
                            CallbackBase.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                    internal void Read(int name, out RemoteException value)
                    {
                        value = new RemoteException();
                        if (map.ContainsKey(name) == false)
                        {
                            Tizen.Log.Error("RPC_PORT", "Not exist key " + name);
                            return;
                        }

                        Unit unit = map[name];
                        if (unit.type != -1223777386 /*remote_exception*/)
                        {
                            Tizen.Log.Error("RPC_PORT", "type is not remote_exception : " + unit.type);
                            return;
                        }
                        TizenActionCalendarCustom.Deserialize(unit.parcel, value);
                    }

                    internal void Write(int name, RemoteException value)
                    {
                        Unit unit = new Unit();
                        unit.name = name;
                        unit.type = -1223777386; /*remote_exception*/
                        unit.cmds.Add((parcel) =>
                        {
                            TizenActionCalendarCustom.Serialize(parcel, value);
                        });

                        map[name] = unit;
                    }

                }

                
                public class CallbackBase
                {
                    internal int Id;
                    internal int SeqId;
                    internal bool Once;
                    private static volatile int _seqNum = 0;

                    public string Tag
                    {
                        get
                        {
                            return Id.ToString() + "::" + SeqId.ToString();
                        }
                    }

                    public CallbackBase(int delegateId, bool once)
                    {
                        Id = delegateId;
                        SeqId = _seqNum++;
                        Once = once;
                    }

                    internal virtual void OnReceivedEvent(UnitMap map) {}

                    internal static void Serialize(Parcel h, CallbackBase param)
                    {
                        h.WriteInt(param.Id);
                        h.WriteInt(param.SeqId);
                        h.WriteBool(param.Once);
                    }

                    internal static void Deserialize(Parcel h, CallbackBase param)
                    {
                        param.Id = h.ReadInt();
                        param.SeqId = h.ReadInt();
                        param.Once = h.ReadBool();
                    }
                }



                
                
                private enum MethodId : int
                {
                    __Result = 0,
                    __Callback = 1,
                    GetEventByIds = 2,
                    SearchInPeriod = 3,
                }

                
                private static void Serialize(Parcel h, TizenEntity param)
                {
                    h.WriteString(param.Id);
                    h.WriteString(param.Extra);
                }

                private static void Deserialize(Parcel h, TizenEntity param)
                {
                    var Id = h.ReadString();
                    param.Id = Id;
                    var Extra = h.ReadString();
                    param.Extra = Extra;
                }

                private static void Serialize(Parcel h, TizenEntityQuery param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Keyword);
                    h.WriteString(param.Category);
                    h.WriteInt(param.Limit);
                }

                private static void Deserialize(Parcel h, TizenEntityQuery param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Keyword = h.ReadString();
                    param.Keyword = Keyword;
                    var Category = h.ReadString();
                    param.Category = Category;
                    var Limit = h.ReadInt();
                    param.Limit = Limit;
                }

                private static void Serialize(Parcel h, TizenEntityCalendarQuery param)
                {
                    Serialize(h, (TizenEntityQuery)param);
                    h.WriteString(param.StartDate);
                    h.WriteString(param.EndDate);
                }

                private static void Deserialize(Parcel h, TizenEntityCalendarQuery param)
                {
                    Deserialize(h, (TizenEntityQuery)param);
                    var StartDate = h.ReadString();
                    param.StartDate = StartDate;
                    var EndDate = h.ReadString();
                    param.EndDate = EndDate;
                }

                private static void Serialize(Parcel h, CalendarEntitySearchQuery param)
                {
                    Serialize(h, (TizenEntityCalendarQuery)param);
                    h.WriteBool(param.SearchTitle);
                    h.WriteBool(param.SearchLocation);
                    h.WriteBool(param.SearchNote);
                }

                private static void Deserialize(Parcel h, CalendarEntitySearchQuery param)
                {
                    Deserialize(h, (TizenEntityCalendarQuery)param);
                    var SearchTitle = h.ReadBool();
                    param.SearchTitle = SearchTitle;
                    var SearchLocation = h.ReadBool();
                    param.SearchLocation = SearchLocation;
                    var SearchNote = h.ReadBool();
                    param.SearchNote = SearchNote;
                }

                private static void Serialize(Parcel h, TizenEntityCalendarEvent param)
                {
                    Serialize(h, (TizenEntity)param);
                    h.WriteString(param.Title);
                    h.WriteString(param.StartDate);
                    h.WriteString(param.EndDate);
                    h.WriteString(param.Note);
                    h.WriteString(param.Location);
                }

                private static void Deserialize(Parcel h, TizenEntityCalendarEvent param)
                {
                    Deserialize(h, (TizenEntity)param);
                    var Title = h.ReadString();
                    param.Title = Title;
                    var StartDate = h.ReadString();
                    param.StartDate = StartDate;
                    var EndDate = h.ReadString();
                    param.EndDate = EndDate;
                    var Note = h.ReadString();
                    param.Note = Note;
                    var Location = h.ReadString();
                    param.Location = Location;
                }

                private static void Serialize(Parcel h, TizenEntityStatus param)
                {
                    h.WriteBool(param.Success);
                    h.WriteString(param.Reason);
                }

                private static void Deserialize(Parcel h, TizenEntityStatus param)
                {
                    var Success = h.ReadBool();
                    param.Success = Success;
                    var Reason = h.ReadString();
                    param.Reason = Reason;
                }

                
                private static void Serialize(Parcel h, List<TizenEntityCalendarEvent> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        Serialize(h, i);
                    }
                }

                private static void Deserialize(Parcel h, List<TizenEntityCalendarEvent> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = new TizenEntityCalendarEvent();
                        Deserialize(h, k);
                        param.Add(k);
                    }
                }

                private static void Serialize(Parcel h, List<string> param)
                {
                    h.WriteArrayCount(param.Count);
                    foreach (var i in param)
                    {
                        h.WriteString(i);
                    }
                }

                private static void Deserialize(Parcel h, List<string> param)
                {
                    int l = h.ReadArrayCount();
                    for (int i = 0; i < l; i++)
                    {
                        var k = h.ReadString();
                        param.Add(k);
                    }
                }

                
                private static void Serialize(Parcel h, RemoteException param)
                {
                    h.WriteInt(param.Cause);
                    h.WriteString(param.Message);
                }

                private static void Deserialize(Parcel h, RemoteException param)
                {
                    param.Cause = h.ReadInt();
                    param.Message = h.ReadString();
                }


                private bool CheckPrivilege(MethodId id, ServiceBase b)
                {
                    if (_privilege_map.ContainsKey(id) == false)
                        return true;

                    var privileges = _privilege_map[id];
                    bool has;
                    foreach (var item in privileges)
                    {
                        has = HasPrivilegeLocal(b.Sender, item);
                        if (has == false) {
                            Tizen.Log.Error("RPC_PORT", "Permission denied. " + b.Sender + " : " + item);
                            return false;
                        }
                    }

                    return true;
                }

                protected override bool OnReceivedEvent(string sender, string instance, Port port)
                {
                    Parcel p;

                    try
                    {
                        p = new Parcel(port);
                    }
                    catch (InvalidIOException)
                    {
                        return false;
                    }

                    return OnReceivedEvent(instance, port, p);
                }

                internal void OnLemReceivedEvent(string instance, Parcel p)
                {
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((TizenActionCalendarCustom)data).OnReceivedEvent(instance, null, p);
                        }, this);
                    }
                    else
                    {
                        OnReceivedEvent(instance, null, p);
                    }
                }

                internal  bool OnReceivedEvent(string instance, Port port, Parcel p)
                {
                    try
                    {
                        ServiceBase b = null;

                        foreach (var i in  _services)
                        {
                            if (i.Instance.Equals(instance))
                            {
                                b = i;
                                break;
                            }
                        }

                        if (b == null)
                        {
                            return false;
                        }

                        Port cb_port;
                        if (b._lem != null)
                            cb_port = null;
                        else
                            cb_port = GetPort(Port.Type.Callback, instance);

                        var result = new Parcel();

                        int cmd;
                        UnitMap map = new UnitMap();
                        map.Deserialize(p);

                        map.Read(1355467489 /*[METHOD]*/, out cmd);

                        switch ((MethodId)cmd)
                        {
                            case MethodId.GetEventByIds:
                            {
                                map.Read(104120 /*ids*/, out List<string> param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.GetEventByIds(param1, out List<TizenEntityCalendarEvent> param2, out List<string> param3);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                        result_map.Write(-995932313 /*unresolvedIds*/, param3);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }
                            case MethodId.SearchInPeriod:
                            {
                                map.Read(-552137728 /*searchQuery*/, out CalendarEntitySearchQuery param1);
                                UnitMap result_map = new UnitMap();
                                if (b._is_app && CheckPrivilege((MethodId)cmd, b) == false)
                                {
                                    RemoteException ex = new RemoteException("Permission denied", (int)Tizen.Internals.Errors.ErrorCode.PermissionDenied);
                                    result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                } else {
                                    try
                                    {
                                        var retVal = b.SearchInPeriod(param1, out List<TizenEntityCalendarEvent> param2);
                                        result_map.Write(1497479973 /*[RESULT]*/, retVal);
                                        result_map.Write(-934426595 /*result*/, param2);
                                    } catch (RemoteException ex)
                                    {
                                        result_map.Write(-1571313492 /*[REMOTE_EXCEPTION]*/, ex);
                                    }
                                }

                                ParcelHeader header = p.GetHeader();
                                ParcelHeader resultHeader = result.GetHeader();
                                resultHeader.SetTag(_tidlVersion + ":2:13");
                                resultHeader.SetSequenceNumber(header.GetSequenceNumber());
                                int ret_cmd = (int)MethodId.__Result;
                                result_map.Write(1355467489 /*[METHOD]*/, ret_cmd);
                                result_map.Serialize(result);

                                if (b._lem != null)
                                    b._lem.SendResult(result);
                                else
                                    result.Send(port);
                                break;
                            }

                            default:
                                return false;
                        }

                        return true;
                    }
                    catch (InvalidIOException)
                    {
                        return false;
                    }
                    finally
                    {
                        p.Dispose();
                    }
                }

                protected override void OnConnectedEvent(string sender, string instance)
                {
                    ServiceBase s = Activator.CreateInstance(_serviceType) as ServiceBase;
                    s.Sender = sender;
                    s.Instance = instance;
                    s.Port = GetPort(Port.Type.Callback, instance);
                    _services.Add(s);
                    s.OnCreate();
                }

                internal void OnLemConnectedEvent(object proxy_instance, string sender, string instance)
                {
                    ServiceBase s = Activator.CreateInstance(_serviceType) as ServiceBase;
                    s.Sender = sender;
                    s.Instance = instance;
                    s._lem = new LocalExecution(proxy_instance);
                    _services.Add(s);
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((ServiceBase)data).OnCreate();
                        }, s);
                    }
                    else
                    {
                        s.OnCreate();
                    }
                }

                protected override void OnDisconnectedEvent(string sender, string instance)
                {
                    foreach (var i in _services)
                    {
                        if (i.Instance.Equals(instance))
                        {
                            if (i._lem != null)
                                i._lem._online = false;

                            i.OnTerminate();
                            _services.Remove(i);
                            break;
                        }
                    }
                }

                internal void OnLemDisconnectedEvent(string sender, string instance)
                {
                    if (Thread.CurrentThread.ManagedThreadId != 1)
                    {
                        _async_context.Post((data) =>
                        {
                            ((TizenActionCalendarCustom)data).OnDisconnectedEvent(sender, instance);
                        }, this);
                    }
                    else
                    {
                        OnDisconnectedEvent(sender, instance);
                    }
                }

                internal static TizenActionCalendarCustom GetInstance()
                {
                    return _instance;
                }

                internal bool GetListenStatus()
                {
                    return _isListen;
                }

                public TizenActionCalendarCustom() : base("TizenActionCalendarCustom")
                {
                    _instance = this;


                }

                /// <summary>
                /// Listens to client apps
                /// </summary>
                /// <param name="serviceType">The type object for making service instances</param>
                /// <exception cref="InvalidIOException">
                /// Thrown when internal I/O error happen.
                /// </exception>
                /// <exception cref="ArgumentException">
                /// Thrown when serviceType is invalid.
                /// </exception>
                public void Listen(Type serviceType)
                {
                    if (!typeof(ServiceBase).IsAssignableFrom(serviceType))
                        throw new ArgumentException("Invalid type");
                    _serviceType = serviceType;
                    Listen();
                    _isListen = true;
                }

                /// <summary>
                /// Gets service objects which are connected
                /// </summary>
                /// <returns>The enumerable service objects which are connected</returns>
                public IEnumerable<ServiceBase> GetServices()
                {
                    return _services;
                }


                protected override void OnTerminatedEvent()
                {
                    foreach (var i in _services)
                    {
                        if (i._lem != null)
                        i._lem._online = false;
                        i.OnTerminate();
                    }
                }

            }
        }
    }
}

